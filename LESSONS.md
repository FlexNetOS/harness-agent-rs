# Harness lessons ledger (harness-agent-rs)

Append-only memory mined from runs by the `evolution-steward` (skill `harness-evolution`).
Recurrence is the signal: a lesson `noted` once becomes an upgrade on its second occurrence.

| Date | Harness | Lesson (generalized class) | Evidence | Recurrence | Routed to | Status |
|------|---------|----------------------------|----------|-----------:|-----------|--------|
| 2026-06-13 | (seed) | Repo created for the harness-agent-rs port; rust-port harness ejected. | — | 0 | — | noted |
| 2026-06-13 | rust-port | The port's own green `cargo test` repeatedly encoded WRONG behavior; only the live differential diff vs the running source caught the downgrade. The gate must be differential (run source via bun + diff), never the port's self-tests. | cycles 1-3: WF-04 u64, WF-01 value-bounds+trim, WF-06 .nullable/.datetime/.date — all green-but-wrong, caught by verifier | 3 | parity-verifier (already enforces); reinforce in porter prompt: "your tests are not the oracle" | UPGRADE |
| 2026-06-13 | rust-port | TS→Rust numeric/string downgrade classes recur per schema unit: `z.number()` w/o `.int()`→f64; `.trim()` is a transform (store trimmed); `.nullable()`≠optional (zod-v4 absent rejects); `.datetime()` Z-only; `z.date()`→chrono [≠]. | WF-04/WF-01/WF-06 | 3 | rust-port-translate skill — add a "zod→serde fidelity checklist"; porter pre-flight audit | propose |
| 2026-06-13 | rust-port | Cartographer's guessed shapes for NEEDS-HUMAN units were wrong (WF-07 node-artifact); porter must read the real file at port time, never trust the ledger's inferred shape. | WF-07 type/url/title guess vs real path/runId/size | 1 | porter prompt already says "read actual source" — keep | noted |
| 2026-06-13 | rust-port | The ledger's PROSE (not just guessed shapes) can be wrong — it mis-stated loadCommandPrompt's search-path precedence, which propagated into the orchestrator's prompt AND the port. Porter+verifier must verify against actual source and fix the ledger. | WF-11 loadCommandPrompt: ledger said configuredFolder-first + invented .claude/commands/; source = configuredFolder LAST + .archon/commands/defaults | 1 | porter/verifier prompts: "the ledger can lie — read source, fix ledger" | noted |
| 2026-06-13 | rust-port | JS↔Rust semantic-fidelity gotchas recur per pure-logic unit and are invisible to the port's own green tests: parseFloat lenient prefix-parse, serde_json preserve_order (insertion order), regex i-flag backreference w/ backtracking, UTF-16 (not byte) string length/slice, zero-width lookahead boundaries. | cycles 4-6: WF-12 parseFloat, WF-13 order, WF-11 backref+utf16+boundary | 5 (class recurs) | rust-port-translate skill: add a "JS→Rust string/number/regex fidelity checklist"; porter pre-flight | propose |
| 2026-06-14 | rust-port | Node `path.join(root, abs)` APPENDS the absolute arg under root; Rust `Path::join(root, abs)` REPLACES root with abs. A path-containment/copy guard ported naively silently drops or escapes — a real downgrade. Use a `node_join()` helper that replicates Node semantics. | cycle 9 IS-06 isPathWithinRoot/copyFiles | 1 | add to JS→Rust fidelity checklist (rust-port-translate) | noted |
| 2026-06-14 | rust-port | Env/global-singleton-mutating tests race under cargo's parallel runner → intermittently flaky GREEN BASELINE (which the harness gates on). Mark them `#[serial_test::serial]`. Latent from cycle 7, surfaced cycle 8 when more env tests raised race probability. | cycle 8: har-paths archon_home_env_var_with_tilde flaked | 1 | porter prompt: "global-env/singleton tests → #[serial]"; baseline-auditor: re-run suspected-flaky 3x | UPGRADE (porter prompt updated) |
| 2026-06-14 | rust-port | Subagents self-report inaccurate test counts ("607 pass") while the real suite has a failure/flake; and rust-analyzer new-diagnostics are often STALE (unresolved-import/type-mismatch on freshly-written files that actually compile). Orchestrator must independently run cargo build+clippy+test, and re-run to catch flakes. | cycles 7-9: stale unlinked-file/serde/Option diagnostics; cycle 8 mis-reported pass count | 3 | orchestrator already verifies independently — keep; don't trust subagent green or IDE diagnostics | noted |
