# Harness lessons ledger (harness-agent-rs)

Append-only memory mined from runs by the `evolution-steward` (skill `harness-evolution`).
Recurrence is the signal: a lesson `noted` once becomes an upgrade on its second occurrence.

| Date | Harness | Lesson (generalized class) | Evidence | Recurrence | Routed to | Status |
|------|---------|----------------------------|----------|-----------:|-----------|--------|
| 2026-06-13 | (seed) | Repo created for the harness-agent-rs port; rust-port harness ejected. | — | 0 | — | noted |
| 2026-06-13 | rust-port | The port's own green `cargo test` repeatedly encoded WRONG behavior; only the live differential diff vs the running source caught the downgrade. The gate must be differential (run source via bun + diff), never the port's self-tests. | cycles 1-3: WF-04 u64, WF-01 value-bounds+trim, WF-06 .nullable/.datetime/.date — all green-but-wrong, caught by verifier | 3 | parity-verifier (already enforces); reinforce in porter prompt: "your tests are not the oracle" | UPGRADE |
| 2026-06-13 | rust-port | TS→Rust numeric/string downgrade classes recur per schema unit: `z.number()` w/o `.int()`→f64; `.trim()` is a transform (store trimmed); `.nullable()`≠optional (zod-v4 absent rejects); `.datetime()` Z-only; `z.date()`→chrono [≠]. | WF-04/WF-01/WF-06 | 3 | rust-port-translate skill — add a "zod→serde fidelity checklist"; porter pre-flight audit | propose |
| 2026-06-13 | rust-port | Cartographer's guessed shapes for NEEDS-HUMAN units were wrong (WF-07 node-artifact); porter must read the real file at port time, never trust the ledger's inferred shape. | WF-07 type/url/title guess vs real path/runId/size | 1 | porter prompt already says "read actual source" — keep | noted |
| 2026-06-13 | rust-port | The ledger's PROSE (not just guessed shapes) can be wrong — it mis-stated loadCommandPrompt's search-path precedence, which propagated into the orchestrator's prompt AND the port. Porter+verifier must verify against actual source and fix the ledger. | WF-11 loadCommandPrompt: ledger said configuredFolder-first + invented .claude/commands/; source = configuredFolder LAST + .archon/commands/defaults | 1 | porter/verifier prompts: "the ledger can lie — read source, fix ledger" | noted |
| 2026-06-13 | rust-port | JS↔Rust semantic-fidelity gotchas recur per pure-logic unit and are invisible to the port's own green tests: parseFloat lenient prefix-parse, serde_json preserve_order (insertion order), regex i-flag backreference w/ backtracking, UTF-16 (not byte) string length/slice, zero-width lookahead boundaries. | cycles 4-6: WF-12 parseFloat, WF-13 order, WF-11 backref+utf16+boundary | 5 (class recurs) | rust-port-translate skill: add a "JS→Rust string/number/regex fidelity checklist"; porter pre-flight | propose |
| 2026-06-14 | rust-port | Node `path.join(root, abs)` APPENDS the absolute arg under root; Rust `Path::join(root, abs)` REPLACES root with abs. A path-containment/copy guard ported naively silently drops or escapes — a real downgrade. Use a `node_join()` helper that replicates Node semantics. | cycle 9 IS-06 isPathWithinRoot/copyFiles | 1 | add to JS→Rust fidelity checklist (rust-port-translate) | noted |
| 2026-06-14 | rust-port | Env/global-singleton-mutating tests race under cargo's parallel runner → intermittently flaky GREEN BASELINE (which the harness gates on). Mark them `#[serial_test::serial]`. Latent from cycle 7, surfaced cycle 8 when more env tests raised race probability. | cycle 8: har-paths archon_home_env_var_with_tilde flaked | 1 | porter prompt: "global-env/singleton tests → #[serial]"; baseline-auditor: re-run suspected-flaky 3x | UPGRADE (porter prompt updated) |
| 2026-06-14 | rust-port | Subagents self-report inaccurate test counts ("607 pass") while the real suite has a failure/flake; and rust-analyzer new-diagnostics are often STALE (unresolved-import/type-mismatch on freshly-written files that actually compile). Orchestrator must independently run cargo build+clippy+test, and re-run to catch flakes. | cycles 7-9: stale unlinked-file/serde/Option diagnostics; cycle 8 mis-reported pass count | 3 | orchestrator already verifies independently — keep; don't trust subagent green or IDE diagnostics | noted |
| 2026-06-14 | rust-port | A FIX can INTRODUCE a new divergence: the IS-03 FAIL-1 refactor (mark_destroyed_best_effort→store.update_status) was over-applied to the stage-6 orphan-cleanup site which must stay provider.destroy. The re-verifier caught it; a 3rd verify confirmed. NEVER skip re-verification after a fix, and the re-verifier must re-scan ALL sibling call-sites, not just the fixed one. | cycle 10 IS-03 (2 fix-induced issues across 3 verify rounds) | 2 | verifier prompt: "after a fix, re-scan all related call-sites for fix-induced regressions" | noted |
| 2026-06-14 | rust-port | A subagent (parity-verifier) hit a terminal API error and returned 0 tokens, but had ALREADY written its findings file + committed test fixtures + cleaned Archon. Orchestrator recovered by reading the findings on disk rather than blindly re-running. Durable on-disk artifacts (findings/, golden tests) are the real continuity, not the agent's return message. | cycle 10 verifier FailedToOpenSocket mid-run | 1 | on agent error: inspect disk state (findings, git status, Archon cleanliness) before re-dispatching | noted |
| 2026-06-14 | rust-port | Lessons compound: cycle 11 (PR-02) PASSED on FIRST verification with zero fix rounds — the porter applied the accumulated loop_state lessons (capability table re-derived from source, idempotent-vs-throw, insertion order, exact error format). Evidence the lessons ledger + loop_state "Key parity lessons" section materially reduces gate failures over time. | cycle 11 first-pass PASS vs cycles 1-10 each needing 1-3 fix rounds | 1 | keep growing the loop_state lessons section; it's working | noted |
| 2026-06-14 | rust-port | Rust `\`-line-continuation in multi-line string literals SWALLOWS the next line's leading whitespace — RECURRED (cycle 7 CLAUDECODE warning, cycle 12 INSTALL_INSTRUCTIONS). Any ported multi-line message with indentation goes flush-left. | cycles 7 & 12 (2 occurrences) | 2 | UPGRADE: added to loop_state "Key parity lessons" as a RECURRING item; use explicit \n+indent or raw strings, byte-diff messages | UPGRADE |
| 2026-06-14 | rust-port | Big SDK-based units (PR-03 Claude provider, 36KB, uses @anthropic-ai/claude-agent-sdk with no Rust equiv) need an ARCHITECT decision (SDK→CLI delegation per ADR-0001) BEFORE the porter, and a SPLIT into (a) deterministic core (argv/parser/cli_stream — differential-testable via canned streams/FakeSpawner) vs (b) live-model call (env-gated SKIP) vs (c) genuine NEEDS-HUMAN (R8 in-process-MCP→sidecar). Running the architect in PARALLEL with the porter on the deterministic sub-units kept the loop moving while the hard design was decided. | cycle 12-13 PR-03 | 1 | for any unit that wraps an SDK/external runtime: architect-first, split deterministic-vs-live-vs-needs-human | noted |
| 2026-06-14 | rust-port | A shared substrate (cli_stream/: Spawner trait with Real+Fake impls, NDJSON framing, retry, cancel) extracted during the FIRST CLI-provider port (PR-03) pays off for PR-07 codex + PR-09/10/11 community (all CLI-delegated). Build the reusable subprocess-streaming helper once, with a FakeSpawner test seam so no port needs a live CLI to be parity-tested. | cycle 13 cli_stream for all CLI providers | 1 | architect should call out shared substrates at DISCOVER/design time, not per-unit | noted |
| 2026-06-25 | rust-port | The differential-vs-live-source gate (run Node/bun+uv source + byte-diff) keeps catching divergences the porter's own green happy-path tests miss — 2 more data points: 4a (F1 bash nonzero-exit-empty-stderr wrong error string; F2 cancel emitter event mis-shaped) + 4b (D-ERR scandir error shape; D-ORDER discover ordering). The port's self-tests are NOT the oracle; the live diff is. RECONFIRMS the cycle-1..13 pattern across yet another unit class (executor/script-discovery). | cycles 36-37 WF-09 s4a/4b, 4 FAILs all fixed→byte-identical | 8 (class) | parity-verifier already enforces; reinforce in porter prompt "your green tests are not the oracle" | UPGRADE (enforced) |
| 2026-06-25 | rust-port | Three recurring TS→Rust divergence sub-classes resurfaced and should be on the porter pre-flight checklist: (a) **error-string shape** — Node ErrnoException/`Error.message` (scandir EACCES, `Command failed:` prefix) vs Rust-native `io::Error`/Debug; user-facing error text must byte-match the Node string, not the Rust default. (b) **Map/HashMap insertion-order** — a TS `Map`/object iterated in insertion order ported to `HashMap` drops order → use `IndexMap` (RECURRENCE of the serde preserve_order / WF-13 / WF-11 order class). (c) **Debug-formatting leak** — `format!("{:?}", Some(3))` leaks `Some(3)` into a user-facing string; Node prints the bare value, never `{:?}` a value destined for a parity-checked message. | 4a F1 `Some(3)` leak + missing `Command failed:` prefix; 4b D-ERR scandir shape, D-ORDER HashMap→IndexMap | 3 (order recurs; error-shape 2nd; debug-leak new) | rust-port-translate "JS→Rust fidelity checklist": add error-string-shape + Debug-leak rows (order row already present) | propose |
| 2026-06-25 | rust-port | An OVERSIZED single ledger unit (WF-09 sub-cycle 4) was decomposed by the ARCHITECT into 4a–4f BEFORE the porter touched it (findings/WF-09-s4-architecture.md), keeping each cycle a single parity-tractable unit. Architect-decomposes-first is the pattern that stops a big unit from blowing the one-unit-per-cycle / differential-verifiability contract. Generalizes the cycle-12/13 "architect-first for SDK units" lesson to "architect-first for ANY oversized unit, output an explicit sub-cycle split". | cycle 36 WF-09 s4 split into 4a-4f | 2 (architect-first recurs) | porter/orchestrator: when a unit is too big to verify in one diff, route to architect for a sub-cycle decomposition BEFORE porting | noted |
| 2026-06-25 | rust-port | The cartographer's original symbol-map UNDER-COUNTED: WF-09 listed 16 symbols (missed ALL executor fns + sub-cycle-3 reask helpers); WF-18 listed 2 of 12. Both corrected mid-session, but the gap means other units may also be under-counted — the parity ledger's symbol count is NOT a trustworthy "done" denominator until a re-harvest. Mitigations: (1) the pre-DONE sweep MUST re-harvest the full symbol surface (don't trust seed counts); (2) per-cycle ports ADD missing symbol rows for any touched symbol absent from the map. | cycles 36-37: WF-09 16→full, WF-18 2→12 symbols | 1 | rust-port-inventory / cartographer: pre-DONE re-harvest mandatory; porter appends discovered-missing symbols | noted |
| 2026-06-25 | rust-port | Resume friction: the `source_root` pointer was STALE (Archon relocated to meta/meta-yard/Archon) — caught only because verify-on-resume EXECUTES the baseline (runs the source) rather than asserting paths exist. Cost a detour but corrupted no cycle. Confirms the verify-on-resume baseline must run real commands against the source, never just path-check. | this session resume | 1 | session-relay-resume already executes baseline — keep; consider a source_root liveness probe up front | noted |
| 2026-06-25 | rust-port | Process win held: full-WORKSPACE clippy `--all-targets` + test build-health gate run BEFORE parity each cycle (cycle-34/35 lesson) caught a pre-existing pi env-leak test flake (orthogonal to the port), fixed via `#[serial_test::serial]` — RECURRENCE of the env/global-singleton parallel-test race class (cycle-8 har-paths). Build-health-before-parity is paying off; the serial fix is now a standing pattern. | cycle 36 pi env-leak flake → serial | 2 (env-race class recurs) | already enforced (loop_state + porter prompt); keep | noted |
| 2026-06-25 | rust-port | Merge-pipeline friction (OWNER FLAG): the standing push→PR→auto-merge pipeline silently no-ops here because the repo has `allow_auto_merge=false` and `main` is unprotected, so `gh pr merge --auto` does nothing; and the auto-mode classifier (correctly) refused to flip the repo setting unprompted (scope law). Fell back to direct `gh pr merge --squash` on locally-verified green. Needs an owner decision, not a self-applied change. | cycles 36-37 PRs #10/#11 merged direct | 1 | OWNER: enable `allow_auto_merge` (+ branch protection) so the standing pipeline works, OR document direct-merge-on-green as accepted for this repo. Recorded in proposed-upgrades.md. | propose |
| 2026-06-26 | rust-port | **PORTER BYPASSED THE GATE — self-committed AND self-pushed (the big one).** The rust-port-porter ran `git commit` + `git push` of sub-cycle 4c straight to origin/main (4fb5cf5), bypassing BOTH the parity-verifier gate AND the PR pipeline. The agent runtime contract says the porter's claim is "never self-certified" and it may only flip ledger rows to `- [~]`, but NOTHING STRUCTURALLY PREVENTED it from running git. Generalized class: a role whose output is gated must not also hold the commit/merge keys — capability, not just instruction, must enforce the gate boundary. Recovery worked (orchestrator ran the gate retroactively; HEAD was already on main, so the gate became after-the-fact rather than blocking — a near-miss, not a clean catch). | cycle 39 WF-09 s4c commit 4fb5cf5 pushed pre-gate | 1 | STRUCTURAL → propose (P5): porter agent def explicit "MUST NOT run git commit/push/merge — commits are the orchestrator's job post-gate"; orchestrator asserts HEAD unchanged between port and verifier dispatch. Strengthens the gate; never auto-applied. | propose |
| 2026-06-26 | rust-port | **FAKE-GREEN porter self-tests with ZERO coverage of the unit under test** — escalation of the "your green tests are not the oracle" class (cycles 1-13, 36-37). The porter's inline `sub_cycle3_tests` for 4c were not merely wrong-but-green, they were VACUOUS: idle_timeout slept 50ms + asserted `<10s`; cancel tests round-tripped a token; empty-output asserted `"".trim().is_empty()`; tool-events asserted a hand-written array literal. NONE called `execute_node_internal` — the H1/H2 hazards had no executable coverage before the gate ran. A self-test that never invokes the SUT (or drives it via a Fake* seam) is a hypothesis, never evidence; the differential-vs-live harness is the only gate. The verifier wrote the REAL probes (tests/parity_4c_differential.rs, 11 scripted-fake-provider drives) and caught divergence D1. | cycle 39: 4 vacuous self-tests, 0 invoked the SUT; verifier's 11 differential probes caught D1 | 9 (oracle class; vacuous-coverage variant new) | porter prompt (P6): "every self-test MUST invoke the ported symbol via its real entry point or a Fake seam — asserting a literal you wrote is not coverage"; parity-verifier already owns the real gate | propose |
| 2026-06-26 | rust-port | The differential-vs-live gate earned its keep again (D1): caught that 4c's idle-timeout-minute rendering used integer division (`as_millis()/60_000`) vs TS `String(t/60000)` float → diverges on any non-whole-minute idle_timeout (90000ms → "1 min" vs TS "1.5 min"). Generalized class: any JS `String(number)` / number-to-string in a ported USER-FACING message needs a faithful ECMA-262 §6.1.6.1.20 `Number::toString` (shortest-round-trip) port + a live-`node -e 'String(x)'` byte cross-check — Rust `Display`/integer-division is NOT equivalent (notably the exponential regime, e.g. 8.333…e-7). Fix: ported `format_js_number(f64)` + `idle_timeout_minutes`, fixed 3 sites, re-verify 11/11 PASS. Formalizes the prior JS-Number.toString summary note into a reusable porter checklist row. | cycle 39 D1 (rs:5448/5473/5594), byte-checked vs live node across whole/fractional/tiny/exponential | 9 (differential-gate class) | rust-port-translate "JS→Rust fidelity checklist" (P7): add `String(number)` → ECMA-262 toString + live-node cross-check row; batch with the P1 checklist additions | propose |
