[package]
name = "har-paths"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Path resolution, env loading, structured logging, telemetry, update-check."

[dependencies]
serde.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
directories.workspace = true
thiserror.workspace = true
dotenvy = "0.15"

[dev-dependencies]
tempfile = "3"
# Serializes env-mutating tests (process-global env like ARCHON_HOME) so they do not race under cargo parallel runner.
serial_test = "3"
