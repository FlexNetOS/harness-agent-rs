[package]
name = "har-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Placeholder core crate for harness-agent-rs (replaced as the port's architecture is established)."
