[package]
name = "har-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Thin re-export facade for downstream convenience (ADR-0001 R1). Re-exports har-contract + har-workflow-schema."

[dependencies]
har-contract = { path = "../har-contract" }
har-workflow-schema = { path = "../har-workflow-schema" }
