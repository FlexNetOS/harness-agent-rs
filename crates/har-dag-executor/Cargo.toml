[package]
name = "har-dag-executor"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "DAG state machine: topo layers, parallel join_all, loop/approval/cancel nodes, session threading."

[dependencies]
har-contract = { path = "../har-contract" }
har-workflow-schema = { path = "../har-workflow-schema" }
har-provider = { path = "../har-provider" }
har-ledger = { path = "../har-ledger" }
har-coord = { path = "../har-coord" }
har-isolation = { path = "../har-isolation" }
har-git = { path = "../har-git" }
thiserror.workspace = true
tokio.workspace = true
tokio-util.workspace = true
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
futures.workspace = true
tracing.workspace = true
