[package]
name = "har-coord"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Run-level locks, resumable-run claim/release, orphan reclamation over weave+grit."

[dependencies]
har-ledger = { path = "../har-ledger" }
thiserror.workspace = true
tokio.workspace = true
async-trait.workspace = true
