[package]
name = "har-db"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "CO database adapter layer (ported from Archon core/db/adapters). Cycle 26: dialect layer only — Database trait query/tx + concrete adapters land cycle 27 after the driver decision."

[dependencies]
uuid = { workspace = true }
async-trait = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
